spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nba_players_db
    username: root
    password: Kingdomain1!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format: true

        # or none

server:
  port: 8080